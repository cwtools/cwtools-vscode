# RING

core_radius_perc = 0.2				# Core radius is 30% of the galaxy radius
ring = {
	width 	= 0.5					# Percentage of radius to use as the width of the ring
	offset 	= 0.4					# Percentage of radius to use as the offset from center for the ring
}