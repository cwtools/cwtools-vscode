namespace = test

planet_event = {
    id = test.1
    is_triggered_only = yes
    trigger = {
        has_resource = { type = mineral amount < 5 }
    }
    immediate = {
        every_tile = { add_deposit = some_resource }
    }
}